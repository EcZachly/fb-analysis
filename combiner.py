class Combiner:
    def __init__(self, path="data/facebook"):
        self.path = path
        print("created a new combiner with path " + self.path)

    def combine(self):
        print("combining files for path:" + self.path)
        return []
